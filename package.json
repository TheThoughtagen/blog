{
  "name": "fieldnotes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A small, terminal-inspired engineering notebook. No runtime dependencies.",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
