{
  "name": "fieldnotes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A small, terminal-inspired engineering notebook.",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@cruciblesoftware/fieldnotes-renderer": "0.1.0"
  }
}
